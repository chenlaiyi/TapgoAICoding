# Tapgo AICoding 开发约定

- 使用简体中文；每完成一个有意义步骤就用 1–3 行报告进度，失败或异常立即说明。
- 收到明确的开发或修复任务后，主动检查、实现并验证，不要只复述需求或停在“下一步做什么”。
- 声称工具不可用前，必须在当前回合实际调用该工具并取得失败证据。
- 开发前先 `git fetch origin`，区分本地工作树、`main` 与 `origin/main`；保留用户未提交改动，避免多台电脑并行覆盖。
- 分开报告源码、测试、App 构建、已安装版本和真实界面回归；其中一层成功不能替代另一层。
- 每次更新完成后，将同一份最新 App 安装到本机、JKmacmini 和 fafamacmini，并分别重启、回读版本与进程状态。
- 长期记忆只保存稳定偏好和项目事实；临时路径、当前版本、未完成任务、思考过程和凭据不得写入长期记忆。

- 长期记忆快照见 `AGENT_MEMORY.md`，由 Codex 会话在每次初始化时刷新；与 `EVOLUTION.md`（版本日志）配合查阅。
