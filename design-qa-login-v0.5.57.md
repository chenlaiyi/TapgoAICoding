# 登录页设计 QA

## 验收范围

- 目标：按用户选定的左右分栏稿实现管理员微信扫码登录页。
- 参考图：`artifacts/login-design-qa/reference-normalized.png`（1144 × 768）。
- 实现图：`artifacts/login-design-qa/implementation-pass3.jpeg`（1144 × 768）。
- 完整并排对照：`artifacts/login-design-qa/side-by-side-pass3.png`。
- 品牌区聚焦对照：`artifacts/login-design-qa/focused-brand-pass3.png`。
- 登录区聚焦对照：`artifacts/login-design-qa/focused-login-pass3.png`。
- 状态：强制显示登录页的只读 QA 模式；二维码加载成功，状态为“等待扫码”，刷新按钮可用。

## 对照结论

- 布局：左右 41.5% / 58.5% 分栏、标题、二维码、状态、刷新入口和右下角版本信息均与选定稿保持同一层级；无重叠、裁切或异常换行。
- 品牌：登录页直接读取 `NSApplication.shared.applicationIconImage`，与已构建 App 的真实图标一致；没有另造 logo、SVG 或占位图。
- 视觉：左侧使用真实位图品牌背景，保持紫蓝渐变、轨迹线和节点；右侧使用高对比浅色背景、白色二维码承载面和轻阴影。
- 字体与间距：标题、说明、状态和操作入口的字号、字重与垂直节奏经过三轮同视口并排对照；最终只保留不同二维码内容、倒计时秒数及 macOS 非活动窗口按钮状态等动态差异。
- 交互：二维码加载、倒计时、扫码轮询、过期/失败重试和手动刷新逻辑均保留；QA 环境变量只强制展示登录页，不绕过认证、不清理登录凭据。
- 可访问性：标题、二维码、刷新按钮和状态保留语义标签；正文与背景对比度清晰，刷新按钮可通过键盘与辅助功能访问。
- 响应性：分栏宽度带最小/最大约束；右侧内容使用可用高度计算顶部留白，低高度窗口不会再与底部版本信息相撞。

## 修正记录

1. 修复初版右侧内容与版本信息重叠，并取消导致标题裁切的固定偏移。
2. 放大真实 App 图标、二维码承载面和主标题，恢复选定稿的视觉主次。
3. 将刷新入口移到等待状态下方，并收紧品牌标题与图标间距。

## 缺陷分级

- P0：0
- P1：0
- P2：0
- P3：0（动态二维码、倒计时和窗口激活态差异不属于实现缺陷）

final result: passed
