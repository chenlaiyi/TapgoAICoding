# Tapgo AICoding 0.5.225

## 补 v0.5.222 `.tool` 过去式回归测试

- 新增 `TurnPresentationTests`：`.tool` 默认 fallback 完成态文案「已使用 <name>」（v0.5.222 对齐 Codex 实机截图 3）。
- 防止 v0.5.222 改动被无意回退。

## 测试

全量回归 3137 通过 + 三机安装回读。
