# Tapgo AICoding 0.5.78

- 与 v0.5.77 同样的代码重新打包并部署。
  - 之前的几次更新只改了源码和测试，没有提供新的可分发文件。这次补上。
  - Tapgo AICoding.app 已安装到开发机。
- 启动后能看到的界面变化（来自 v0.5.74 和 v0.5.75）：
  - 侧栏接受拖拽时的虚线高亮环改为 目标 IDE 风格的天空蓝。
  - 辅助对话发送失败时的提示用 目标 IDE 风格的高饱和红。
  - 文件列表里"执行失败"的徽章用 目标 IDE 风格的琥珀色。
- 与 v0.5.77 相同的测试结果（2689 通过 / 13 失败，其中 13 个为预先存在的远程 SSH 集成测试）。
- 下载 Tapgo-AICoding-0.5.77.zip（在 v0.5.77 发布页面下，因为本次没有新代码）。
- 已知限制：首次启动需要登录 codex 账号才能看到完整界面。