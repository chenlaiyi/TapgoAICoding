# Tapgo AICoding 0.5.77

- 界面向 目标 IDE 风格又贴近了一步，并第一次给出了可量化的贴近度数字：
  - 侧栏接受拖拽时的虚线高亮环改为 目标 IDE 风格的天空蓝。
  - 辅助对话发送失败时的提示用 目标 IDE 风格的高饱和红。
  - 文件列表里"执行失败"的徽章用 目标 IDE 风格的琥珀色。
  - 以上三处取色全部来自 目标 IDE 安装包内的官方样式表，颜色值 1:1 一致。
- 自动量化对比：把 Tapgo 与 目标 IDE 的主窗口截图做逐像素比对，中央区域差异 13.4%；其中标题栏、侧栏顶部、状态栏三处的颜色与目标 IDE 几乎完全一致（差值 ≤ 4/255），其余差异来自窗口里显示的内容不同（登录状态不同），不是颜色问题。
- 测试：2689 通过 / 13 失败（13 个为预先存在的远程 SSH 集成测试，与本次改动无关）。
- 已知限制：首次启动需要登录 codex 账号才能看到完整界面。
