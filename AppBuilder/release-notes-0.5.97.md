# Tapgo AICoding 0.5.97

- 延续 0.5.96 的 ZCode 式手机 Web Remote 重构，并修复真实数据验收中发现的历史任务不可见问题。
- 没有 `projectId` 的旧任务会按 `cwd` 自动归回已配置工作区；已移除项目的历史目录以只读工作区卡片呈现，没有目录信息的任务统一进入“未分类任务”。
- 历史工作区仍可展开和进入任务，但不会显示无效的“新建任务”按钮；页面总任务数与实际可见任务重新一致。
- 新增 cwd 子目录匹配、历史目录分组、无目录兜底和活动项目归类回归，完整测试为 2731 passed、0 failed。
