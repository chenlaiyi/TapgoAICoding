# Tapgo AICoding 0.5.114

- 修复 SSH 能连接但找不到远端执行器的问题，统一端口与密钥配置。
- 远端确认工作目录；配置缺失时明确报错，不切换到本机执行。
- 远程项目不再混入客户端的项目记忆，顶部展示主机与目录。
- 回复更简洁，减少文件清单、重复确认与行内代码的视觉干扰。

验证覆盖双向真实 SSH 执行器握手、远端命令、目录解析与界面深浅色回归。开发者签名有效；尚未完成 Apple 公证。
