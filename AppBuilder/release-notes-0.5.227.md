# Tapgo AICoding 0.5.227

## 补 v0.5.220 压缩图标回归测试

- 新增 `TurnPresentationTests`：`.compaction` 活动图标 `arrow.triangle.2.circlepath`（对齐 Codex 实机截图 1）。
- 防止 v0.5.220 改动被无意回退。

## 测试

全量回归 3140 通过 + 三机安装回读。
