# Tapgo AICoding 0.5.220

## 上下文压缩活动图标对齐 Codex 实机（循环箭头）

- Codex 实机截图 1「上下文已自动压缩」前的图标是循环箭头（↔ 风格）。
- 此前 Tapgo 用 `text.line.last.and.arrowtriangle.forward`（text →），换成 `arrow.triangle.2.circlepath`（双向循环箭头），更贴合「压缩/循环」语义。

## 测试

全量回归 3125 通过 + 三机安装回读。
