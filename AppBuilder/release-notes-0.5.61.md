# Tapgo AICoding 0.5.61

- 新增 ZCode 式独立右侧工作台：辅助对话、审查、多终端和浏览器标签可同时打开，支持标签检索、逐个关闭、关闭其他标签及整体收起；每个辅助标签拥有独立、可持久化且可并行运行的 Harness 会话。
- 审查页展示真实文件 Diff；终端支持在当前项目目录运行及停止 zsh 命令；浏览器支持地址导航、前进、后退、刷新和外部打开。
- 环境信息升级为右侧二级抽屉，可点按钮或从最右缘向右拖出；工作台宽度、环境宽度和标签状态会自动恢复。
- 任务顶栏对齐 ZCode 的 Finder、打开方式、帮助、终端和侧边面板操作顺序。
